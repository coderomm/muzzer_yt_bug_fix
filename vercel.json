{
    "builds": [
      {
        "src": "package.json",
        "use": "@vercel/node",
        "config": {
          "buildCommand": "npm run prisma-generate && next build"
        }
      }
    ],
    "routes": [
    {
      "src": "/(.*)",
      "dest": "client/$1"
    }
  ]
  }
  